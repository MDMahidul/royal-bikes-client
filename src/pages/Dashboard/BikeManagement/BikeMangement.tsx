import SectionHeader from "@/components/SectionHeader/SectionHeader";

const BikeMangement = () => {
    return (
        <div>
            <SectionHeader heading="Manage Bikes"/>
        </div>
    );
};

export default BikeMangement;